<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  </li>
<% end %>

<% @all_users.each do |user| %>
  <%= link_to 'profile', admin_profile_path(id: user.id) %>
<% end %>
